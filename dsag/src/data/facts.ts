export const facts = [
    "Arrays in Java are objects and are dynamically allocated on the heap.",
    "The time complexity of accessing an element in an array by index is O(1).",
    "A linked list uses more memory than an array because it needs to store both the data and the reference to the next node.",
    "Java's HashMap uses a technique called 'separate chaining' to handle collisions.",
    "The worst-case time complexity of quicksort is O(n²), but its average case is O(n log n).",
    "In Java, the String class is immutable, meaning once created, its value cannot be changed.",
    "A binary heap can be efficiently represented using an array, without needing explicit pointers.",
    "The Fibonacci sequence can be computed in O(log n) time using matrix exponentiation.",
    "Java's garbage collector automatically reclaims memory occupied by unreferenced objects.",
    "A balanced binary search tree guarantees O(log n) time complexity for search, insert, and delete operations.",
    "Dynamic programming is a technique that solves complex problems by breaking them down into simpler subproblems.",
    "In Java, the 'final' keyword can be applied to classes, methods, and variables with different effects.",
    "The amortized time complexity of adding an element to an ArrayList is O(1), even though occasionally it requires O(n) time to resize.",
    "Breadth-First Search (BFS) uses a queue data structure, while Depth-First Search (DFS) uses a stack.",
    "Java's TreeMap implements a Red-Black tree, which is a type of self-balancing binary search tree.",
    "The space complexity of recursive algorithms includes the stack space used by recursive calls.",
    "Greedy algorithms make locally optimal choices at each step with the hope of finding a global optimum.",
    "In Java, primitive types are stored on the stack, while objects are stored on the heap.",
    "The Floyd-Warshall algorithm can find the shortest paths between all pairs of vertices in a weighted graph.",
    "Java's ConcurrentHashMap provides thread-safe operations without locking the entire map.",
  ]
  
  